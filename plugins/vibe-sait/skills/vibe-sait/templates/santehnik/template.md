# Шаблон: сантехник, электрик (вызов на дом)

Ниша 7 «Сантехник, электрик (вызов на дом)» (`references/ниши.md`). Подходит и для мастера на час, установки бытовой техники, аварийной службы дома. Мир: документальный (преамбул №3 из `references/миры-и-промпты.md`, дневной свет, честные поверхности). Грамматика: **таймлайн вызова** – страница как история одного вызова от «течёт» до «сделано»: заявка → интервал и цена → мастер выехал → на месте, цена до начала → сделано, гарантия. Одна кнопка на весь сайт: «Вызвать мастера» → `{{ссылка-записи}}` (мессенджер: Telegram `https://t.me/…`, WhatsApp `https://wa.me/…`, MAX). Телефон – текстовой ссылкой `tel:{{телефон}}` в шапке, в hero («или позвоните») и в финале. Если владелец принимает только звонки, в слот `{{ссылка-записи}}` подставить `tel:{{телефон}}` – подпись кнопки не меняется.

Компания в каркасе: «Кран и Щиток». Никакой географии: город, зона выезда, адрес – слоты.

## Три «П»

- **Боль:** течёт или искрит прямо сейчас, «мастер будет завтра после обеда», цена всплывает после работы, нужного не оказалось с собой, после ухода снова течёт.
- **Персона:** взрослый 25–65 в состоянии «срочно», ищет с телефона «сантехник рядом вызвать», решает за минуту по одному критерию: приедет быстро и назовёт цену.
- **Обещание:** приезд в оговорённый интервал (`{{время-выезда}}`), цена на типовые работы известна до приезда, с собой стандартные детали, гарантия на работу письменно (`{{гарантия}}`).

## Акты

| # | Акт | Устройство | Зачем |
|---|---|---|---|
| 1 | Hero `#top`: кухня, открытый шкаф под мойкой, таз под сифоном. Лейбл, H1, лид, кнопка, «или позвоните», плашка часов и интервала справа сверху | `pin` 2,4 экрана (1,8 на телефоне), два плана параллакса: фон `hero` / `hero-m` (−0,7) и карточка-фото `card` с руками мастера (−0,28), `kinetic="lines"` на H1, реплика «встречает» `0 0.82 0`, скрим углом `v-scrim--lead`, «пол» градиентом | Узнавание: своя кухня и ведро под сифоном, оффер «интервал и цена» и кнопка над сгибом. Трекер внизу едет 0 → 1 |
| 2 | «Течь не ждёт до завтра» `#poka`: три строки боли (стяжка, розетка, «цена после») и карточка «Поэтому» с поворотом | `flow` + `data-v-in` (stagger 70), фото `stain` со шторкой `reveal="right"` (sticky на десктопе) | Цена бездействия и поворот: почему интервал и цену называем до выезда. Трекер держит 1 |
| 3 | «Как проходит вызов» `#kak`: три статуса-реплики с чипом «Статус · …» (интервал и цена → мастер выехал → на месте) | `pin` 2,6 экрана (2,2 на телефоне), фото `work` с параллаксом −0,5, три реплики встык (`0 0.3 0 0.05`, `0.28 0.72 0.05 0.05`, `0.7 1 0.05 0`), первая видна уже на p = 0 | Доказательство: что происходит между сообщением и «сделано». Трекер едет 1 → 3 |
| 4 | Цены на типовые работы `#ceny`: две колонки строк «Сантехника / Электрика» по пять работ, строка «Выезд и осмотр», сумка sticky | `flow` + `data-v-in` (stagger 60), фото `bag` sticky | Выбор: цена до приезда, что типовое, что считаем отдельно. Трекер держит 3 |
| 5 | «Что обещаем письменно» `#garantiya`: четыре факта строками, фото нового смесителя | `flow` + `reveal="left"` на фото `done` + `data-v-in` | Обещание: цена не меняется, детали с собой, гарантия, сухо и чисто. Трекер едет 3 → 4 |
| 6 | Отзывы с Яндекс.Карт `#otzyvy` (`data-slot-optional`) | `flow` + `data-v-in` (stagger 90), одна крупная цитата и две малые, звёзды CSS-ом по `--r` | Доверие: только настоящие отзывы слотами. Нет отзывов – секцию удалить целиком |
| 7 | Вызов `#vyzov`: «Напишите, что случилось, и приложите фото», кнопка, телефон, часы, интервал, зона, адрес, футер с юрлицом | `flow` + `data-v-in`, `spotlight` на секции, `magnet` на кнопке | Действие: одно сообщение с фото. Трекер стоит на «Сделано» |

Семейства: pin, parallax, kinetic, reveal, flow + in, cursor (spotlight, magnet) и свой ход от `--v-p`. Без pan и count. Длина около 11 экранов на 1440×900. Наверху страницы `data-v-progress`, drift между `#1c1f23`, `#20242a`, `#181b1f`.

## Фирменный ход

Трекер вызова «Где сейчас мастер» (`<aside class="track">`), как трекер такси или доставки: пять остановок (Заявка · Интервал и цена · Выехал · На месте · Сделано), маркер-пилюля едет по линии, пройденные остановки загораются акцентом, текст статуса меняется с затуханием («Ждём вашу заявку» → «Интервал и цена названы» → «Мастер выехал» → «На месте: цена до начала» → «Сделано, гарантия в акте»).

Позицию считает скрипт в конце `index.html`: карта «акт → от какой остановки к какой» (`top` 0→1, `poka` 1, `kak` 1→3, `ceny` 3, `garantiya` 3→4, `otzyvy` 4, `vyzov` 4). Текущий акт – последний, чей верх выше середины экрана; у pin-актов доля пути берётся из `--v-p`, у flow-актов считается от положения секции относительно середины экрана. Секция, удалённая при сборке (`#otzyvy`), пропускается сама.

На компьютере трекер – полоса внизу экрана на всю ширину (статус слева, линия с подписями справа), видна всегда, финал получает `padding-bottom` под неё. На телефоне – нижняя панель: статус и линия точек слева, кнопка «Вызвать мастера» справа; панель появляется, когда кнопка hero ушла за верх экрана, и прячется в финале (там кнопка своя). Пока панель видна, кнопка в шапке скрыта (`.v-page.track-on`), чтобы на экране была одна.

## Палитра и шрифты

| Переменная | А «Графит + оранжевый» (в index.html) | Б «Бетон + оранжевый» (светлая) | В «Графит + вода» |
|---|---|---|---|
| `--v-canvas` | `#1c1f23` | `#eeedea` | `#171b20` |
| `--v-surface` | `#272b31` | `#ffffff` | `#21262d` |
| `--v-ink` | `#f0eeea` | `#1f2226` | `#eef1f4` |
| `--v-ink-soft` | `#a4a8ae` | `#5f646b` | `#9aa4ae` |
| `--v-accent` | `#f07a1f` | `#b04408` | `#6cc7e6` |
| `--v-accent-ink` | `#1a0f05` | `#fff6ee` | `#0b1a22` |
| `--v-shadow-color` | `8 10 13` | `31 34 38` | `6 9 13` |

Контраст (WCAG, посчитано по hex):

| Пара | А | Б | В |
|---|---|---|---|
| ink на canvas | 14,3:1 | 13,6:1 | 15,3:1 |
| ink на surface | 12,3:1 | 16,0:1 | 13,4:1 |
| ink-soft на canvas | 6,9:1 | 5,1:1 | 6,8:1 |
| ink-soft на surface | 6,0:1 | 6,0:1 | 6,0:1 |
| accent на canvas (цены, чипы) | 5,9:1 | 4,9:1 | 9,0:1 |
| accent-ink на accent (кнопка) | 6,7:1 | 5,4:1 | 9,2:1 |

- **А «Графит + оранжевый»:** палитра ниши из справочника: тёмный графит, аварийный оранжевый. Родная для документальных дневных кадров: фото светлые, страница тёмная, контраст «спокойная страница, тревожный кадр».
- **Б «Бетон + оранжевый»:** светлая версия для мастеров, которые хотят «дневной», спокойный сайт: холст цвета бетона, тёмный оранжевый, чтобы держать 4,5:1 на светлом. Скримы движка (`v-scrim--lead`) остаются тёмными под светлым текстом на фото – hero и «Как проходит» не перекрашивать; в остальных секциях текст тёмный сам по себе.
- **В «Графит + вода»:** тот же графит, акцент – холодная вода вместо огня. Для сантехников без электрики, для «спокойной» подачи. Оранжевый в кадрах не участвует (документальный свет нейтральный), поэтому картинки те же.

Кроме `--v-*` цвет захардкожен в местах, которые при смене палитры поменять руками:

| Где | Что стоит (А) | На что менять |
|---|---|---|
| `html, body { background }` | `#1c1f23` | `--v-canvas` палитры |
| `.bar` градиент и `.bar.is-scrolled`, `.track` подложка, `.chip` подложка | `rgb(28 31 35 / …)` | `--v-canvas` в RGB |
| `.hero__floor` | `rgb(28 31 35 / .55)` | `--v-canvas` в RGB |
| `.track__marker` тень, `.chip i` кольцо, `.close` spotlight | `rgb(240 122 31 / …)` | `--v-accent` в RGB |
| `data-v-drift` у семи актов | `#1c1f23`, `#20242a`, `#181b1f` | три остановки в семье нового холста (для Б: `#eeedea`, `#e8e7e2`, `#f2f1ed`) |
| favicon в `<link rel="icon">` | `%23f07a1f` и `%231c1f23` | акцент и холст |
| Тёмные подписи на фото (`figcaption`, `.poka__fig::after` и т.п.) | `rgb(0 0 0 / …)`, `color: var(--v-ink)` | для светлой Б подписи на фото задать `color: #f0eeea` явно |

Проверка после смены: `grep -nE '#[0-9a-f]{6}|rgb\(' index.html` не должен находить старых цветов.

Шрифты: Oswald 500–600 (дисплей, узкий, «служебный», хорошо держит короткие статусы) + Onest 400–600 (текст), обе с кириллицей, обе в библиотеке Тильды. Подключение – `<link>` Google Fonts в `<head>`; в Тильде пара выбирается в настройках сайта, `<link>` не нужен. Цены набраны текстовым шрифтом (Onest 600), чтобы знак ₽ не зависел от дисплейного.

## Варианты первого экрана

**А (как в `index.html`): два плана параллакса.** `pin` 2,4. Фон – кухня с открытым шкафом и тазом под сифоном (`assets/hero.webp`, на телефоне `assets/hero-m.webp` через `<picture>`, порог 860 px), едет со скоростью −0,7; поверх справа внизу карточка-фото рук мастера с подводкой и ключом (`assets/card.webp` / `card-480.webp`, 3:4, −0,28, со своей тенью). Текст в левом нижнем углу на скриме `v-scrim--lead`: лейбл, кинетический H1 построчно, лид, кнопка и «или позвоните {{телефон}}». Справа сверху плашка «Принимаем вызовы {{часы}} · Интервал приезда {{время-выезда}}» (на телефоне скрыта, эти слоты повторяются в финале). На телефоне карточка стоит справа сверху на пустой стене, текст внизу на полосе-скриме. Оба плана видны без скролла, при прокрутке кухня уходит вдаль быстрее карточки.

**Б: шторка «шкаф высох».** `pin` 2,4 + `reveal`. Нижний слой – тот же кадр с течью (`hero` / `hero-m`), поверх него в `<figure data-v-reveal="up" data-v-reveal-at="0 0.45">` кадр после ремонта: тот же ракурс, сухой шкаф, новый сифон, таза нет (`assets/hero-b.webp` / `hero-b-m.webp`, записи манифеста с `"variant": "Б"`, image-to-image по референсу `raw/hero.png` и `raw/hero-m.png`, чтобы ракурс совпал). При первом же движении шторка снизу вверх «высушивает» шкаф. Текст слева внизу на скриме, как в А; карточка рук не используется. Трекер в этот момент едет 0 → 1: «шкаф высох» и «интервал назван» происходят одновременно.

**В: реплики-статусы поверх одного кадра.** `pin` 2,8, один полноэкранный кадр `work` (мастер у открытого шкафа), фон не едет, а медленно приближается: `transform: scale(calc(1 + var(--v-p) * .08))` на `.v-layer`. Три реплики встык в левом верхнем углу на пустой стене кадра (`0 0.36 0 0.05`, `0.34 0.68 0.05 0.05`, `0.66 1 0.05 0`), каждая с чипом статуса и `data-v-kinetic="words"`: «Написали, что течёт», «Интервал и цена до выезда», «Мастер на месте, сделано, гарантия». Оффер (H1 + кнопка + телефон) держится всегда (`0 1 0 0`) на полосе `v-scrim--band` снизу. Первый кадр уже с репликой, экран не пустой. Новых сцен нет; на телефоне используется `hero-m` (кадр `work` горизонтальный).

## Углы оффера

1. **Боль** (стоит в `index.html`). H1: «Сантехник и электрик на дом в {{город}}: цена до приезда». Подзаголовок: «Течёт или искрит? Напишите, что случилось, и пришлите фото – ответим интервалом приезда и ценой на типовую работу. Детали с собой.»
2. **Персона.** H1: «Сантехник рядом, в {{город}}: интервал приезда и цена сразу». Подзаголовок: «Позвоните или напишите: назовём интервал и цену на типовую работу до выезда, приедем со стандартными деталями.»
3. **Обещание.** H1: «Сантехник и электрик в {{город}} – гарантия на работу письменно». Подзаголовок: «Устраним течь, заменим смеситель, розетку или автомат. Цена до начала работы, стандартные детали с собой, гарантия – в акте.»

Правило первого экрана (12.09.2026): h1 = что делаем + для кого (город слотом) + одно УТП, до 9 слов; условия и гарантии – в подзаголовке. Лейбл над H1 – типовые работы («Течь, смеситель, унитаз, розетка, автомат · выезд на дом»), og:title = title; слот {{город}} из лейбла переехал в H1.

## Слоты

Все `{{…}}` из `index.html` (проверено grep-ом, 27 разных слотов):

| Слот | Где | Что подставить |
|---|---|---|
| `{{город}}` (7) | description, og:description, JSON-LD (2), лейбл hero, адрес в финале, футер | город в нужном падеже («Казани» в «в {{город}}», «Казань» в лейбле и футере) – проверить падеж в каждом месте |
| `{{адрес сайта}}` (4) | `og:url`, `og:image`, JSON-LD `url` и `image` | полный адрес сайта без слэша на конце |
| `{{телефон}}` (7) | шапка (ссылка и подпись), hero «или позвоните» (ссылка и подпись), финал (ссылка и подпись), JSON-LD | номер клиента; в `href="tel:"` без пробелов и скобок |
| `{{ссылка-записи}}` (4) | кнопки «Вызвать мастера»: шапка, hero, финал, панель трекера на телефоне | `https://t.me/…`, `https://wa.me/…`, MAX или `tel:{{телефон}}`, если только звонки |
| `{{часы}}` (3) | плашка hero, финал, JSON-LD `openingHours` | часы приёма вызовов («08:00 – 22:00 без выходных»); «круглосуточно» только если это правда |
| `{{время-выезда}}` (3) | плашка hero, реплика «интервал и цена» в `#kak`, финал | интервал приезда как условие услуги («в течение 2 часов», «60–90 минут в будни») |
| `{{зона-выезда}}` (2) | абзац цен, финал | районы или радиус («весь город и до 20 км за МКАД») |
| `{{цена}}` (12) | 10 строк типовых работ (5 сантехника, 5 электрика, по порядку сверху вниз), «Выезд и осмотр», `priceRange` в JSON-LD | цифры из прайса клиента; «от» и ₽ уже в разметке |
| `{{гарантия}}` (1) | факт «Гарантия на работу …» | срок гарантии на работу («12 месяцев») |
| `{{адрес}}` (2) | JSON-LD, строка «Адрес» в финале (`data-slot-optional`) | адрес офиса или склада; нет – удалить строку из финала и `streetAddress` из JSON-LD |
| `{{юрлицо}}`, `{{ИНН}}` (по 1) | футер (`data-slot-optional`) | ИП или ООО и ИНН; нет – удалить `<span>` |
| `{{рейтинг}}` (3) | цифра, `--r` звёзд, aria-label | рейтинг с Яндекс.Карт: через запятую в тексте («4,8»), через точку в `--r` (4.8) |
| `{{отзывов}}` (1) | «… отзывов» | число отзывов на карточке |
| `{{ссылка-карты}}` (1) | «Читать все отзывы» | адрес карточки организации на Яндекс.Картах |
| `{{отзыв 1..3}}`, `{{имя 1..3}}`, `{{дата 1..3}}`, `{{оценка 1..3}}` (оценки по 2: `--r` и aria-label) | три цитаты в `#otzyvy` | копипаста с карточки без правок, до 220 знаков; оценка 1–5. Нет отзывов – удалить секцию `#otzyvy` целиком, трекер пропустит её сам |

Без фигурных скобок, но тоже сверить с владельцем: название «Кран и Щиток» (шапка, футер, `<title>`, `og:site_name`, JSON-LD), список работ и примечания к ним (что именно берут, «штробление отдельно», «согласуем отключение стояка с УК»), условие «выезд и осмотр засчитываются в стоимость работы», перечень деталей «с собой» в трёх местах (реплика `#kak`, подпись сумки, факт в `#garantiya`).

## Правила текста

- Не писать: «мастер на час» как название услуги, «любая сложность», «дёшево», «круглосуточно» без подтверждения, «профессионально», «лучшие мастера», «под ключ».
- Слова ниши: вызов, интервал приезда, цена до начала работы, гарантия на работу, с собой, устранить течь, заменить, типовая работа, акт.
- Никакой статистики и выдуманных чисел: «10 лет», «5000 вызовов», «98% довольных», стаж мастера. Цифры только как условия услуги слотами: `{{время-выезда}}`, `{{часы}}`, `{{гарантия}}`, `{{цена}}`. «Приедем за час» писать только если владелец подтвердил это как условие – тогда это значение слота `{{время-выезда}}`.
- Отзывы только копипастой с Яндекс.Карт, не редактировать. Нет отзывов – секцию удалить, а не оставлять плейсхолдеры.
- Никакой географии в шаблоне: город, зона выезда и адрес только слотами.
- Одна кнопка = одна подпись: «Вызвать мастера» везде; телефон – текстовая ссылка, не вторая кнопка.
- Статусы трекера и чипы «Статус · …» в `#kak` – одна и та же лексика (заявка, интервал и цена, выехал, на месте, сделано); менять только парами.
- Тире только короткое «–».

## Сборка

1. Генерация по `images.json`: семь записей базы, `hero-m` идёт после `hero` по референсу (тот же шкаф в портретном кадре). Записи варианта Б (`hero-b`, `hero-b-m`) – только если выбран первый экран Б (`images.mjs … --variant Б`). Каждую картинку посмотреть глазами до сжатия: текст на упаковках и бутылках, лица, лишние предметы на месте текста – реролл.
2. `sharp` по `outputs`: hero 1920, телефон 1080, карточка 900/480, фото секций 1600/900 и 1200/800, webp q76–78. `og.jpg` – кроп `hero` 1200×630 по центру, без текста, отдельной генерации нет. Бюджет `assets/` – до 1,5 МБ.
3. Обходы движка, которые уже в `index.html`: `img` внутри `<picture>` в слое позиционируется своим CSS (`.hero__back img`, `.hero__card img`), потому что селектор движка `.v-layer > img` до него не достаёт; на телефоне `data-v-span` hero и `#kak` переписываются скриптом до `Vibe.mount` (1,8 и 2,2); на телефоне подписи с `--v-t-sm` (14 px на 390 px) подняты до 0,95rem отдельным медиазапросом, чтобы кегль текста был не меньше 15 px; трекер читает `--v-p` через `getComputedStyle` и сам следит за `scroll`/`resize`; шапка получает подложку `.is-scrolled` после 40 px.
4. После заполнения слотов: `shoot.mjs` на 1440×900 и 390×844, проверить, что реплики доходят, горизонтальной прокрутки нет, и что на телефоне панель трекера не перекрывает кнопку финала. Перед отправкой клиенту проверить, что в html не осталось ни одного `{{`.
